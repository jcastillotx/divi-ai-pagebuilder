<?xml version="1.0"?>
<ruleset name="Divi AI Page Builder Coding Standards">
    <description>PHP coding standards for Divi AI Page Builder plugin.</description>

    <!-- What to scan -->
    <file>.</file>

    <!-- Exclude paths -->
    <exclude-pattern>/vendor/*</exclude-pattern>
    <exclude-pattern>/node_modules/*</exclude-pattern>
    <exclude-pattern>/assets/dist/*</exclude-pattern>
    <exclude-pattern>/tests/*</exclude-pattern>
    <exclude-pattern>*.min.js</exclude-pattern>
    <exclude-pattern>*.min.css</exclude-pattern>

    <!-- How to scan -->
    <arg value="sp"/>
    <arg name="basepath" value="."/>
    <arg name="colors"/>
    <arg name="extensions" value="php"/>
    <arg name="parallel" value="8"/>

    <!-- Rules: WordPress Coding Standards -->
    <rule ref="WordPress">
        <!-- Allow short array syntax -->
        <exclude name="Generic.Arrays.DisallowShortArraySyntax"/>
        <!-- Allow filename without class prefix for main plugin file -->
        <exclude name="WordPress.Files.FileName.InvalidClassFileName"/>
    </rule>

    <!-- WordPress Extra rules -->
    <rule ref="WordPress-Extra"/>

    <!-- WordPress Docs rules -->
    <rule ref="WordPress-Docs"/>

    <!-- Check for PHP cross-version compatibility -->
    <config name="testVersion" value="8.0-"/>
    <rule ref="PHPCompatibilityWP"/>

    <!-- Set minimum WordPress version -->
    <config name="minimum_supported_wp_version" value="6.0"/>

    <!-- Text domain for i18n -->
    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="divi-ai-pagebuilder"/>
            </property>
        </properties>
    </rule>

    <!-- Prefix for functions and classes -->
    <rule ref="WordPress.NamingConventions.PrefixAllGlobals">
        <properties>
            <property name="prefixes" type="array">
                <element value="divi_ai"/>
                <element value="DiviAI"/>
                <element value="DIVI_AI"/>
            </property>
        </properties>
    </rule>

    <!-- Escape output functions -->
    <rule ref="WordPress.Security.EscapeOutput">
        <properties>
            <property name="customEscapingFunctions" type="array">
                <element value="divi_ai_escape_json"/>
            </property>
        </properties>
    </rule>
</ruleset>
